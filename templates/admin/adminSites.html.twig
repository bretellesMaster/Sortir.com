{% extends 'base.html.twig' %}

{% block title %}Gérer les sites{% endblock %}

{% block body %}
<div class="container">
    <form action="" method="post">
    <label for="site-search">Search the site:</label>
    <input type="search" id="site-search" name="q"
           aria-label="Search through site content">
    <button class="btn btn-primary buttonAdminSites">Rechercher</button>

            <hr>

        <table class="table table-dark">
            <thead>
            <tr>
                <th colspan="2">Villes</th>
                <th scope="col">&nbsp;</th>
                <th scope="col">&nbsp;</th>
                <th scope="col">Actions</th>
            </tr>
            </thead>
            <tbody>

                {% for sortie in sorties %}
                <tr>
                <td>{{ sortie.site }}</td>
                <td></td>
                <td><a href="{{ path('sortieUpdate', {'id': sortie.id}) }}">Modifier</a></td>
                <td><a href="{{ path('sortieDelete', {'id': sortie.id }) }}">Supprimer</a></td>
                </tr>
                    {% endfor %}

            </tbody>
        </table>



        <div class="buttonUserDetails">
            <a class="btn btn-primary buttonUserDetails" href="{{ path ('main') }}" role="button">Ajouter</a>
        </div>
    </form>
</div>
{% endblock %}